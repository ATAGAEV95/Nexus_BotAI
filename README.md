# Nexus Bot AI

Универсальный шаблон Discord бота на Python 3.13+, использующий современные технологии и лучшие практики.

## 🛠 Технический стек

*   **Python 3.13+**
*   **uv** — управление зависимостями
*   **discord.py** — взаимодействие с Discord API
*   **PostgreSQL + asyncpg + SQLAlchemy** — база данных
*   **aiohttp** — асинхронные HTTP запросы
*   **APScheduler** — планировщик задач

## 🚀 Установка и запуск

### Локально

1.  **Установите uv**:
    ```bash
    curl -LsSf https://astral.sh/uv/install.sh | sh
    ```

2.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/your-repo/nexus-bot-ai.git
    cd nexus-bot-ai
    ```

3.  **Установите зависимости**:
    ```bash
    uv sync
    ```

4.  **Настройте окружение**:
    Скопируйте пример файла конфигурации:
    ```bash
    cp .env.example .env
    ```
    Откройте `.env` и заполните `DC_TOKEN`, `POSTGRES_*` и другие переменные.

5.  **Запустите бота**:
    ```bash
    uv run main.py
    ```

### Docker

Проект содержит готовый `Dockerfile` для production-среды.

1.  **Соберите образ**:
    ```bash
    docker build -t nexus-bot-ai .
    ```

2.  **Запустите контейнер**:
    ```bash
    docker run -d --env-file .env --name nexus-bot nexus-bot-ai
    ```

## 🧪 Тестирование

Для запуска тестов используйте:
```bash
uv run pytest
```

Для проверки окружения ("Sanity Check"):
```bash
uv run main_test.py
```

## 📁 Структура проекта

```text
app/
├── cogs/       # Команды и модули
├── core/       # Основные компоненты (Bot, Config, Embeds)
├── data/       # Работа с БД (Models, Requests)
├── services/   # Сервисы (Scheduler)
└── tools/      # Утилиты
tests/          # Тесты
```
